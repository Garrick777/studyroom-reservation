# 🎨 UI设计规范 - 智慧自习室

> **设计风格**: Bento Grid 现代风格  
> **图标库**: Lucide Icons  
> **确认日期**: 2026-01-21

---

## 一、设计理念

### 核心概念
- **Bento Grid** - 类Apple风格的不规则网格布局
- **渐变色块** - 每个功能区域独特的渐变配色
- **大号排版** - 数字突出、层次分明
- **微交互** - 悬浮动效增强体验

### 设计关键词
`现代` `清新` `活力` `专注` `游戏化`

---

## 二、颜色体系

### 主色 - 学术绿
```scss
$primary: #3FB19E;        // 主色
$primary-dark: #2F8B7C;   // 深色
$primary-light: #E8F5F3;  // 浅色背景
```

### 辅助色
```scss
$accent: #FFCB2F;         // 金色强调
$accent-orange: #FF7A45;  // 活力橙
$purple: #7C5CFF;         // 成就紫
$pink: #FF6B9D;           // 活力粉
$blue: #4A9FFF;           // 管理蓝
```

### 语义色
```scss
$success: #00C48C;        // 成功/空闲
$warning: #FFB020;        // 警告/暂离
$error: #FF4D4F;          // 错误/异常
$info: #4A9FFF;           // 信息/使用中
```

### 中性色
```scss
$gray-50: #FAFBFC;        // 页面背景
$gray-100: #F4F5F7;       // 卡片悬浮背景
$gray-200: #E8EAED;       // 边框/分割线
$gray-300: #D1D5DB;
$gray-400: #9CA3AF;       // 占位符文字
$gray-500: #6B7280;       // 次要文字
$gray-600: #4B5563;       // 正文
$gray-700: #374151;
$gray-800: #1F2937;       // 标题
$gray-900: #111827;       // 强调标题
```

### 渐变预设
```scss
// 英雄区渐变
$gradient-hero: linear-gradient(135deg, #3FB19E 0%, #2DD4BF 50%, #4A9FFF 100%);

// 功能卡片渐变
$gradient-green: linear-gradient(135deg, #3FB19E, #2DD4BF);
$gradient-orange: linear-gradient(135deg, #FF7A45, #FF9F45);
$gradient-purple: linear-gradient(135deg, #7C5CFF, #A78BFA);
$gradient-pink: linear-gradient(135deg, #FF6B9D, #F472B6);
$gradient-blue: linear-gradient(135deg, #4A9FFF, #60A5FA);
$gradient-yellow: linear-gradient(135deg, #FFCB2F, #FDE047);

// 深色计时器背景
$gradient-dark: linear-gradient(135deg, #1A1A2E 0%, #16213E 100%);
```

---

## 三、布局规范

### 学生端 - Bento Grid

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────────────────────────┐  ┌─────────────────────┐ │
│  │   英雄区 (渐变背景)          │  │   用户状态卡        │ │
│  │   grid-column: 1.2fr         │  │   grid-column: 0.8fr│ │
│  └──────────────────────────────┘  └─────────────────────┘ │
│                                                             │
│  ┌─────────────────┐ ┌────────┐ ┌────────────────────────┐ │
│  │  计时器 (span-2) │ │ 打卡   │ │     成就展示          │ │
│  └─────────────────┘ └────────┘ └────────────────────────┘ │
│                                                             │
│  ┌────────────────────────────┐ ┌────────────────────────┐ │
│  │   自习室列表 (span-2)       │ │   目标 (span-2)       │ │
│  │   row-span: 2              │ ├────────────────────────┤ │
│  │                             │ │   排行榜              │ │
│  └────────────────────────────┘ └────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 管理员端 - 控制台布局

```
┌──────────────────────────────────────────────────────────────┐
│  ┌────┐  ┌────────────────────────────────────────────────┐ │
│  │    │  │   头部: 标题 + 实时状态指示器                   │ │
│  │ 图 │  ├────────────────────────────────────────────────┤ │
│  │ 标 │  │   统计卡片 (4列)                               │ │
│  │ 导 │  ├────────────────────────────┬───────────────────┤ │
│  │ 航 │  │   座位监控 (3D透视网格)     │   实时动态流     │ │
│  │    │  │                             │                   │ │
│  └────┘  └────────────────────────────┴───────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 超管端 - 数据中心布局

```
┌──────────────────────────────────────────────────────────────┐
│  ┌────────────────────────────────────────────────────────┐ │
│  │   头部: Logo + 标题                                     │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │   数据卡片 (4列, 顶部彩条)                              │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │   功能模块 (4列 x 2行)                                  │ │
│  │   悬浮边框高亮 + 待办徽章                               │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

---

## 四、组件规范

### 圆角
```scss
$radius-sm: 12px;   // 小按钮、标签
$radius-md: 16px;   // 普通卡片
$radius-lg: 24px;   // 大卡片
$radius-xl: 32px;   // 英雄区、主卡片
$radius-full: 50%;  // 头像、徽章
```

### 阴影
```scss
$shadow-sm: 0 2px 8px rgba(0,0,0,0.04);
$shadow-md: 0 4px 16px rgba(0,0,0,0.08);
$shadow-lg: 0 8px 32px rgba(0,0,0,0.12);
$shadow-card: 0 2px 16px rgba(0,0,0,0.04);
$shadow-hover: 0 12px 32px rgba(0,0,0,0.08);
```

### 间距
```scss
$spacing-xs: 4px;
$spacing-sm: 8px;
$spacing-md: 16px;
$spacing-lg: 24px;
$spacing-xl: 32px;
$spacing-2xl: 48px;
```

### 卡片样式
```scss
.bento-card {
  background: white;
  border-radius: $radius-lg;
  padding: 24px;
  box-shadow: $shadow-card;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  
  &:hover {
    transform: translateY(-4px);
    box-shadow: $shadow-hover;
  }
}
```

---

## 五、图标规范

### 图标库
**Lucide Icons** - https://lucide.dev/

### 安装
```bash
npm install lucide-vue-next
```

### 使用
```vue
<script setup>
import { Home, Calendar, Target } from 'lucide-vue-next'
</script>

<template>
  <Home :size="20" />
</template>
```

### 尺寸规范
| 场景 | 尺寸 |
|------|------|
| 导航图标 | 20-22px |
| 卡片图标 | 24px |
| 功能图标 | 26-28px |
| 大型展示 | 30-32px |
| 状态图标 | 14-18px |

### 常用图标映射

| 功能 | 图标 |
|------|------|
| 首页 | `Home` |
| 自习室 | `BookOpen` / `Building2` |
| 预约 | `Calendar` / `CalendarPlus` |
| 签到 | `ScanLine` |
| 签退 | `LogOut` |
| 暂离 | `Clock3` |
| 目标 | `Target` |
| 成就 | `Trophy` |
| 排行榜 | `Medal` |
| 好友 | `Users` |
| 小组 | `UsersRound` |
| 商城 | `ShoppingCart` / `ShoppingBag` |
| 消息 | `Bell` / `MessageCircle` |
| 设置 | `Settings` |
| 座位 | `Armchair` |
| 火焰 | `Flame` |
| 积分 | `Coins` |
| 星星 | `Star` |
| 皇冠 | `Crown` |

---

## 六、座位状态颜色

| 状态 | 背景渐变 | 边框色 | 文字色 |
|------|----------|--------|--------|
| 空闲 | `#E8F5F3 → #D1FAE5` | `#3FB19E` | `#3FB19E` |
| 使用中 | `#DBEAFE → #BFDBFE` | `#4A9FFF` | `#4A9FFF` |
| 暂离 | `#FEF3C7 → #FDE68A` | `#FFB020` | `#FFB020` |
| 异常 | `#FEE2E2 → #FECACA` | `#FF4D4F` | `#FF4D4F` |
| 不可用 | `#F4F5F7` | `#E8EAED` | `#9CA3AF` |

---

## 七、成就徽章等级

| 等级 | 渐变色 | 效果 |
|------|--------|------|
| 普通 | `#B0BEC5 → #78909C` | 无 |
| 稀有 | `#64B5F6 → #1976D2` | 微光 |
| 史诗 | `#BA68C8 → #7B1FA2` | 光晕 |
| 传说 | `#FFD54F → #FF8F00` | 金光闪烁 |

---

## 八、动效规范

### 缓动函数
```scss
$ease-default: cubic-bezier(0.4, 0, 0.2, 1);
$ease-bounce: cubic-bezier(0.34, 1.56, 0.64, 1);
```

### 过渡时间
```scss
$duration-fast: 0.2s;
$duration-normal: 0.3s;
$duration-slow: 0.4s;
```

### 常用动效
```scss
// 卡片悬浮
.card:hover {
  transform: translateY(-4px);
  box-shadow: $shadow-hover;
}

// 按钮点击
.btn:active {
  transform: scale(0.98);
}

// 图标旋转 (加载)
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

// 脉冲 (实时状态)
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

// 火焰闪烁
@keyframes flicker {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.8; transform: scale(1.1); }
}
```

---

## 九、响应式断点

```scss
$breakpoint-sm: 640px;   // 手机
$breakpoint-md: 768px;   // 平板竖屏
$breakpoint-lg: 1024px;  // 平板横屏/小笔记本
$breakpoint-xl: 1280px;  // 笔记本
$breakpoint-2xl: 1536px; // 大屏
```

---

## 十、文件结构

```
frontend/src/
├── assets/
│   ├── styles/
│   │   ├── variables.scss    # 变量定义
│   │   ├── mixins.scss       # 混入
│   │   ├── base.scss         # 基础样式
│   │   ├── components.scss   # 组件样式
│   │   └── main.scss         # 入口文件
│   └── icons/                # 本地图标 (可选)
├── components/
│   ├── common/
│   │   ├── AppIcon.vue       # 图标组件
│   │   ├── BentoCard.vue     # Bento卡片
│   │   └── ...
│   └── ...
└── ...
```

---

**预览文件**: `docs/layout-creative.html`  
**图标速查**: `docs/lucide-preview.html`  
**更新日期**: 2026-01-21
